package com.stayFit.dailyNutrition;

import java.time.LocalDate;

import com.stayFit.enums.FitnessState;
import com.stayFit.enums.Gender;
import com.stayFit.enums.Goal;
import com.stayFit.repository.DBConnector;

public class Test {	
	/*public static void main(String[]args) {
		DailyNutritionController dc = new DailyNutritionController(new DailyNutritionalCreateUseCase(new DailyNutritionDAO(new DBConnector())));
		
		try {
			dc.create(new UserInfoDTO(Gender.MASCHIO, Goal.METTERE_MASSA_MUSCOLARE, 
					FitnessState.MEDIAMENTE_ATTIVO, LocalDate.of(1979,06,25), 175, 69, 1));
		}
		catch(Exception ex) {
			
		}
	}*/
}
