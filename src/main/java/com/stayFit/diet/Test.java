package com.stayFit.diet;

/*import java.time.LocalDate;

import com.stayFit.enums.FitnessState;
import com.stayFit.enums.Gender;
import com.stayFit.enums.Goal;
import com.stayFit.repository.DBConnector;*/

public class Test {	
	/*public static void main(String[]args) {
		DietController dc = new DietController(new CreateDietUseCase(new CreateDietDAO(new DBConnector())));
		
		try {
			dc.create(new UserInfoDTO(Gender.MASCHIO, Goal.METTERE_MASSA_MUSCOLARE, 
					FitnessState.MEDIAMENTE_ATTIVO, LocalDate.of(2002,01,20), 200, 100, 10));
		}
		catch(Exception ex) {
			
		}
	}*/
}
